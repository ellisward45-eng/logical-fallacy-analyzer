import requests

url = "http://127.0.0.1:5000/analyze"
data = {"text": "You're wrong because you always say that! Only dumb people believe that."}

response = requests.post(url, json=data)
print(response.json())
